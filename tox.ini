[tox]
envlist = py37,py38,py39,mypy,pre-commit

[testenv]
deps = pytest
commands =
    pytest

[testenv:pre-commit]
deps =
    pre-commit
commands =
    pre-commit run --all-files

[testenv:mypy]
deps =
    mypy
    types-setuptools
commands =
    mypy src
